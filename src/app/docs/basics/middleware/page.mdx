# Middleware

Middleware is a powerful feature in Nexy that allows you to intercept and modify requests before they reach your route handlers. This gives you fine-grained control over request processing and enables common functionality like authentication, logging, and request transformation.

## Basic Usage

To create middleware, define a function that takes a `Request` object and returns a modified request or response:
