# Installation

<Mark>
   <Badge>Nexy</Badge> s'installe globalement sur votre système. Une fois installé, vous pouvez l'utiliser dans tous vos projets sans avoir à le réinstaller.
</Mark>

Avant de vouloir installer <Badge>Nexy</Badge> , veillez vous assurer que vous disposez d'une version de <br/><Badge>Python >= 3.12</Badge>
Cette vérifiaction est nécessaire pour utiliser <Badge>Nexy</Badge> et profiter de toutes ses fonctionnalités modernes.

---


## Comment vérifier la version de Python ?
Pour vérifier la version de Python, ouvrez votre terminal et exécutez la commande suivante :

<CodeCard fileName="Terminal">
<Code language="shell"
 theme="catppuccin-mocha"
code = {
`Python --version`
}
/>
<Code 
language="txt"
theme="catppuccin-mocha"
canCopy={false}
code = {
`Python 3.12.0`
}
/>
</CodeCard>

<Mark>
    Si votre version de Python n'est pas à jour, téléchargez la dernière version depuis le site officiel de Python : 
    <MDXLink href="https://www.python.org/downloads/" >python.org/downloads </MDXLink>. Suivez les instructions d'installation pour votre système d'exploitation.
</Mark>
---- 

## Comment installer Nexy ?
Une fois que vous avez vérifié que vous disposez d'une version de <Badge>Python >= 3.12</Badge>, vous pouvez installer <Badge>Nexy</Badge> en utilisant la commande suivante :

<CodeCard fileName="Terminal">
<Code language="shell"
 theme="catppuccin-mocha"
code = {
`pip install nexy`
}/>

</CodeCard>

----

## Verifier Nexy est bien installe ?

Pour vérifier que <Badge>Nexy</Badge> est bien installé, ouvrez votre terminal et exécutez la commande suivante :

<CodeCard fileName="Terminal">
<Code language="shell"
 theme="catppuccin-mocha"
code = {
`nexy --version`
}/>
<Code
language="txt"
theme="catppuccin-mocha"
canCopy={false}
code = {
`Nexy 0.1.8`
}/>
</CodeCard>

----
## 👌 Bravo !
Maintenant que <Badge>Nexy</Badge> est installé, vous pouvez commencer à créer vos premiers projets !<br/> Nous allons voir comment faire cela dans la section suivante.



<SwitchPage
    prev = { {
        label: "Introduction",
        href: "/"
    }}
    next ={ {
        label: "Créer un projet",
        href: "/started/new_project"
    }}

/>
